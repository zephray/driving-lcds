#pragma once

// Target resolution
#define TITLE "IMGVIEW"
#define TARGET_FPS (30)
#define DISP_WIDTH (312)
#define DISP_HEIGHT (230)
#define WINDOW_WIDTH (DISP_WIDTH * 2 + 1)
#define WINDOW_HEIGHT (DISP_HEIGHT * 2)
#define ENABLE_LPF
